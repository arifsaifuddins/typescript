"use strict";
const person = {
    id: 2384773822,
    name: 'arief',
    isStudent: true,
    // age:34 // optional field
};
// interface Points = string // can't to define one value with equal
const p1 = 23;
const hasilTambah = (num1, num2) => num1 + num2;
const hasilKurang = (num1, num2) => num1 - num2;
console.log(hasilTambah(12, 34));
const arief = (fn, ln) => fn + ln;
console.log(arief('arief', 'saifuddien'));
