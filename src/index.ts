/**
 * // run typescript by tsc command 
 * - tsc --init (initialization)
 * - tsc (compile)
 * - tsc --watch (watching)
*/

/**
 * // types of data
 * - number
 * - string
 * - boolean
 * - any // anything of types
 * - void // no return function
 */

console.log('Typescript Basic')