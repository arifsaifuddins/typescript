// typevar
type Ikan = string
const ikan: Ikan = 'arwana'

// type to define array
type Names = any[]
const names = [23, 'arief', true]

// type to define object types
type User = {
  id: number
  name: string
}

const user: User = { // assign User type to user object
  id: 3203092,
  name: 'adian'
}